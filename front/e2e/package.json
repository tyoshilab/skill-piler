{
  "name": "skill-piler-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:debug": "jest --verbose"
  },
  "dependencies": {
    "puppeteer": "^22.15.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"],
    "setupFilesAfterEnv": ["<rootDir>/setup.js"],
    "testTimeout": 60000,
    "verbose": true,
    "maxWorkers": 1
  }
}